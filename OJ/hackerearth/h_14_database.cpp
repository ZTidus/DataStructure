// h_14_database
// 1. 一个麻烦的地方在于输出表格，得找到一列中最长的那个作为标准，然后进行输出（看到题
// 目后的想法。
// 2. 这道题先放在这里
#include <cstdio>
int main(){
    int T; // Number of tables.
    int m, n;  // The number attributes, the number of touples
    scanf("%d", &T);
    for(int i = 0; i < T; i++){

    }
    return 0;
}