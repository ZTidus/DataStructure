#include <cstdio>

int cmp(int a, int b) {
    return a < b;
}

int main() 
{
    int t;  // number of test cases
    for(int i = 0; i < t; i++) {
        int n;  // coach numbers
        scanf("%d", &n);

    }

}

